version: 0.2
phases:
  install:
    commands:
      - aws cloudformation package --template-file teamlate.yaml --s3-bucket devops-poc-artifact --output-template-file packaged-template.yml
  #build:
    #commands:
      #- pip install --upgrade pip
      #- pip install pipenv --user
      #- pipenv install awscli aws-sam-cli
      #- pipenv run sam package --template-file template.yml --s3-bucket devops-poc-artifact --output-template-file packaged-template.yml
artifacts:
  files:
    - teamlate.yaml
    - packaged-template.yml
